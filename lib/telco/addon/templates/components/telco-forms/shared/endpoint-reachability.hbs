<h3 class="au-display-md">Reach</h3>
{{dds/checkbox-array
  items=(array
    (hash key="intra-regional" label="Intra-regional")
    (hash key="inter-regional" label="Inter-regional")
  )
  field=(mut product.location_destination_reach)
  wide=true
}}
<div class="clear-both"><br><br></div>
<div class="col-sm-18">
  <h3 class="au-display-md"> Location </h3>
  {{dds/checkbox-array
    items=(array
      (hash key="metro" label="Metro")
      (hash key="regional" label="Regional")
      (hash key="remote" label="Remote")
    )
    field=(mut product.intra_regional_locations)
    disabled=(not intraRegionalChecked)
    break=true
  }}
  <br>
</div>
<div class="col-sm-18">
  <h3 class="au-display-md"> Destination </h3>
  {{dds/checkbox-array
    items=(array
      (hash key="metro" label="Metro")
      (hash key="regional" label="Regional")
      (hash key="remote" label="Remote")
    )
    field=(mut product.inter_regional_destinations)
    disabled=(not interRegionalChecked)
    break=true
  }}
</div>
