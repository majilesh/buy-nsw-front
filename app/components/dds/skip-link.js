import Component from '@ember/component';
import layout from '../../templates/components/dds/skip-link';

export default Component.extend({
  layout,
  tagName: 'nav',
  classNames: ['au-skip-link']
});
