import Component from '@ember/component';
import layout from '../../templates/components/dds/template-body';

export default Component.extend({
  layout,
  classNames: ['container-fluid less-space']
});
