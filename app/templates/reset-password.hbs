{{#dds/template-body auth=auth signout=true}}
  <div class="col-sm-3">
  </div>
  <div class="col-sm-30">
    <h1 class="au-display-xl">Change your password</h1>
    <br>
    <p>
      {{dds/new-password
        wide=true
        allRow=true
        tip="Passwords must be at least 8 characters long. Tip: create a password with 3 or more words – it's secure and easy to remember."
        field=(mut password)
        confirm=true
        valid=(mut passwordValid)
        enter=(if (v-get this "isValid") (action "submit"))
      }}
    </p>
    {{#if apiError.email}}
      {{dds/validation-error float="right" type="error" message=apiError.email}}
    {{/if}}
    <p>
      <span class="float-right">
        {{dds/primary-button
          onClick=(action "submit")
          disabled=(v-get this "isInvalid")
          label="Change my password"
        }}
      </span>
    </p>
  </div>
{{/dds/template-body}}
