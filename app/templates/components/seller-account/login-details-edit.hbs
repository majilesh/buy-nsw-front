{{dds/text-input
  isSimple=true
  label-text="Email address"
  inputClass="maxw-300"
  field=(mut form.email)
  showError=true
  invalid=(v-get form "email" "isInvalid")
  errorMessage=(v-get form "email" "message")
  apiError=apiError.email
}}

{{dds/new-password
  isSimple=true
  inputClass="maxw-300"
  field=(mut form.newPassword)
  allowBlank=true
  valid=(mut form.passwordValid)
}}
