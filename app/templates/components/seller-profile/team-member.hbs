{{#dds/accordion-container
  title="Team members"
  subtitle="Do you have team leads with specialist technical knowledge?"
  subtitleBold="Tip:"
  theme="sapphire"
  expanded=false
}}
  {{#if underEdit}}
    {{seller-profile/edit-save
      theme="sapphire"
      underEdit=(mut underEdit)
      form=form
    }} 
    {{seller-profile/team-member-edit form=form}}
  {{else}}
    <div class="row">
      {{#dds/info-box class="col-xs-18"}}
        {{fa-icon "info-circle" prefix="fas"}} Only available to registered buyers
      {{/dds/info-box}}
      {{seller-profile/edit-save
        class="col-xs-18"
        theme="sapphire"
        underEdit=(mut underEdit)
        form=form
      }} 
    </div>
    <div class="cool-infobox sapphire">
      You can add and update your accreditations, engagements and awards at any time.
    </div>
    {{seller-profile/team-member-view form=form buyerView=true}}
  {{/if}}
  {{seller-profile/edit-save
    theme="sapphire"
    underEdit=(mut underEdit)
    form=form
  }}
{{/dds/accordion-container}}
