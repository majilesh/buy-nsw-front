{{#dds/accordion-container
  title="Team members"
  subtitle="𝗧𝗶𝗽: Do you have team leads with specialist technical knowledge?"
  theme="sapphire"
  expanded=expanded
}}
  {{#if underEdit}}
    {{seller-profile/edit-save
      class="a-r"
      theme="sapphire"
      underEdit=(mut underEdit)
      form=form
    }} 
    {{seller-profile/team-member-edit form=form}}
  {{else}}
    <div class="row" id="team-member">
      {{#dds/info-box class="col-xs-27"}}
        {{fa-icon "info-circle" prefix="fas"}} Only available to registered buyers
      {{/dds/info-box}}
      {{seller-profile/edit-save
        class="col-xs-9"
        theme="sapphire"
        underEdit=(mut underEdit)
        form=form
      }} 
    </div>
    <div class="cool-infobox sapphire">
      You can add and update team members at any time.
    </div>
    {{seller-profile/team-member-view form=form buyerView=true}}
  {{/if}}
  {{seller-profile/edit-save
    class="a-r"
    theme="sapphire"
    underEdit=(mut underEdit)
    form=form
  }}
{{/dds/accordion-container}}
