<div class="row">
  <div class={{if allRow "col-sm-12 text-input-label" "col-sm-4 text-input-label"}}>
    <label for="text-input">
      {{label-text}}
      {{yield}}
    </label>
  </div>
  <div class={{if allRow "col-sm-12" "col-sm-8"}}>
    {{input
      class=(if wide "au-text-input au-text-input--block" "au-text-input")
      value=(mut field)
      type=(if password "password" "text")
      disabled=disabled
      pattern=pattern
      focus-out=focusOut
      enter=enter
      placeholder=placeHolder
      autocomplete=(if noAutoComplete "new-password" "true")
    }}
  </div>
</div>
