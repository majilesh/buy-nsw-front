<label for="textarea">{{label-text}}</label>
{{textarea
  class="au-text-input au-text-input--block"
  focus-out=focusOut
  key-up = (action (mut hasChanged) true)
  value=(mut field)
  maxlength=maxLength
}}
{{#if maxLength}}
  <p class="gray">
    {{sub maxLength (if field (get field "length") 0)}} characters remaining
  </p>
{{/if}}
{{#if (and (or showError hasChanged) invalid)}}
  <span class="field-error error">{{errorMessage}}</span>
  <div class="clear-both"></div>
{{/if}}
